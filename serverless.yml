service: omicas
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x

plugins:
 - serverless-offline
 
functions:
  hello:
    handler: 'src/functions/handler.hello'
    events:
      - http:
          method: get
          path: hello
